let { DataBaseTool } = require("../DataBaseTool");
let expect = require("chai").expect;

/*
describe('DB', function() {
    describe('#connect()', function() {
        it('dbt not be undefined', function() {
        	let dbt = DataBaseTool.instance();
        	dbt.then(connection => {
        		expect(connection).to.be.a('promise');
        	});
            
        })
    })
})
*/

