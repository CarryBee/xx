class FinanceBox {
    constructor(info) {
        this.userid = info._id;
        this.sign = info.sign;
        this.number = info.number;
        this.name = info.projectname;

    }

    

    cashFlow(conntx) { // 事务上下文

    }
}